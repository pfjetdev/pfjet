# ✈️ Генерация данных для самолетов (Aircraft)

## 📋 Обзор

Этот скрипт автоматически генерирует профессиональный контент для всех самолетов из файла `Aircarft.md`, включая:

- ✅ Описания (короткие и полные)
- ✅ Спецификации (пассажиры, дальность, скорость, багаж, размеры кабины)
- ✅ Список особенностей (features)
- ✅ Фотографии из Unsplash
- ✅ Сохранение в Supabase

## 🎯 Что уже готово:

1. ✅ SQL схема для таблицы `aircraft` (`supabase-aircraft-schema.sql`)
2. ✅ Скрипт генерации данных (`generateAircraftData.ts`)
3. ✅ Парсинг файла `Aircarft.md` (все категории и модели)

## 📊 Статистика по самолетам:

- **Turboprop**: 4 самолета
- **Very Light**: 4 самолета
- **Light**: 14 самолетов
- **Midsize**: 7 самолетов
- **Super-mid**: 9 самолетов
- **Heavy**: 10 самолетов
- **Ultra Long**: 8 самолетов
- **VIP airliner**: 4 самолета

**Всего: ~60 самолетов**

## 🚀 Как запустить:

### Шаг 1: Создать таблицу в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `scripts/supabase-aircraft-schema.sql`
4. Вставьте в SQL Editor и нажмите **Run**

Вы должны увидеть сообщение об успешном создании таблицы `aircraft`.

### Шаг 2: Проверить .env.local

Убедитесь, что у вас есть все необходимые ключи в `.env.local`:

```env
# Supabase
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_ANON_KEY=ваш-ключ

# OpenRouter (для AI генерации)
OPENROUTER_API_KEY=sk-or-v1-ваш-ключ

# Unsplash (для фотографий)
UNSPLASH_ACCESS_KEY=ваш-ключ

# Дополнительно
YOUR_SITE_URL=http://localhost:3000
YOUR_SITE_NAME=Private Jet Travel
```

### Шаг 3: Запустить генерацию

```bash
npx tsx scripts/generateAircraftData.ts
```

## ⏱️ Время выполнения:

- **~60 самолетов** × **6-8 секунд каждый** = **~6-8 минут**

Скрипт автоматически делает паузы между запросами для соблюдения rate limits.

## 📂 Структура данных

Каждый самолет будет содержать:

```typescript
{
  name: "King Air 260",
  slug: "king-air-260",
  category: "Turboprop",
  category_slug: "turboprop",
  description: "Короткое описание (100-150 символов)",
  full_description: "Полное описание (250-350 символов)",
  specifications: {
    passengers: "Up to 9",
    range: "1,720 nm",
    speed: "310 mph",
    baggage: "55 cu ft",
    cabin_height: "4.8 ft",
    cabin_width: "4.5 ft"
  },
  features: [
    "Advanced Avionics",
    "Executive Seating",
    "Full Refreshment Center",
    // ... 6 features total
  ],
  gallery: ["url1", "url2", "url3"],
  image: "main_image_url"
}
```

## 🔍 Проверка данных

После генерации проверьте данные в Supabase:

```sql
-- Посмотреть количество самолетов по категориям
SELECT category, COUNT(*) as count
FROM aircraft
GROUP BY category
ORDER BY count DESC;

-- Посмотреть все самолеты
SELECT name, category, passengers, range, speed
FROM aircraft
ORDER BY category, name;

-- Посмотреть конкретную категорию
SELECT * FROM aircraft
WHERE category = 'Turboprop';
```

## 💰 Стоимость

**Полностью БЕСПЛАТНО!**

- ✅ **OpenRouter** - модель `google/gemini-2.0-flash-exp:free` бесплатная
- ✅ **Unsplash** - 50 запросов в час бесплатно
- ✅ **Supabase** - бесплатный tier

## 🎨 Особенности

### AI генерация (OpenRouter):
- Профессиональные luxury описания
- Реалистичные спецификации на основе реальных данных
- 6 уникальных features для каждого самолета

### Фотографии (Unsplash):
- Поиск по названию самолета
- Fallback на "luxury private jet" если не найдено
- Выбор фото с максимальным количеством лайков

### Supabase:
- Автоматический upsert (обновление существующих записей)
- Row Level Security для безопасности
- Индексы для быстрых запросов

## 🔧 Дополнительные опции

### Обработать только одну категорию:

Отредактируйте `generateAircraftData.ts` и добавьте фильтр:

```typescript
// В функции main(), после parseAircraftFile()
const categoryToProcess = 'Turboprop'; // Или любая другая категория

// Оставить только нужную категорию
const filtered = new Map();
if (aircraftByCategory.has(categoryToProcess)) {
  filtered.set(categoryToProcess, aircraftByCategory.get(categoryToProcess)!);
}
aircraftByCategory = filtered;
```

### Пропустить Unsplash (только AI генерация):

Закомментируйте вызов `getAircraftPhoto`:

```typescript
// const photoUrl = await getAircraftPhoto(aircraftName);
const photoUrl = null; // Использовать placeholder
```

## ⚠️ Troubleshooting

### Ошибка "Rate limit exceeded" (Unsplash)
- Unsplash позволяет 50 запросов в час
- Подождите 1 час и запустите снова
- Скрипт продолжит с того места, где остановился (благодаря upsert)

### Ошибка "Invalid JSON from AI"
- Иногда AI может вернуть невалидный JSON
- Скрипт автоматически пропустит такой самолет
- Можете запустить скрипт снова для пропущенных

### Ошибка подключения к Supabase
- Проверьте `SUPABASE_URL` и `SUPABASE_ANON_KEY`
- Убедитесь, что таблица `aircraft` создана
- Проверьте RLS политики в Supabase

## 🎯 Следующие шаги

После генерации всех данных:

1. **Интегрировать с UI**:
   ```typescript
   // Вместо хардкода в page.tsx
   const { data: aircraft } = await supabase
     .from('aircraft')
     .select('*')
     .eq('category_slug', categorySlug)
     .eq('slug', modelSlug)
     .single();
   ```

2. **Создать страницу списка самолетов**:
   ```typescript
   const { data: aircraft } = await supabase
     .from('aircraft')
     .select('*')
     .order('category', 'name');
   ```

3. **Добавить поиск**:
   ```typescript
   const { data } = await supabase
     .from('aircraft')
     .select('*')
     .ilike('name', `%${searchQuery}%`);
   ```

4. **Добавить больше фото в gallery** (опционально)

## 📝 Примечания

- Скрипт безопасен для повторного запуска (используется upsert)
- Все данные генерируются AI на основе реальных моделей самолетов
- Спецификации максимально точные благодаря обучению AI
- Можно в любой момент обновить отдельные самолеты вручную в Supabase

---

**Готово!** 🎉 Если есть вопросы или проблемы, проверьте логи в консоли или откройте issue.
